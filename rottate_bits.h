#ifndef TEST_H
#define TEST_H
/*the function rotate(val,rot) that returns the value of the integer val and rotated by rot bit positions 
@param value of the integer to input from the user
return rotated by rot bit positions*/

void print_in_all_bases(unsigned int a);
unsigned int rotate(unsigned int a, int b) ;
unsigned int rotate_right(unsigned int a, int b);
void print_base(unsigned int a, int base);
int count_bits_of_un_int();
#endif /*TEST_H*/





